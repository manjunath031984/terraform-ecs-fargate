# ECS Service Module

Creates the TRS Fargate service using FARGATE_SPOT-first capacity strategy, rolling ECS deployments, circuit breaker rollback, ECS Exec, CloudWatch Logs, secrets, and ALB target registration.
